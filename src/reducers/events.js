import {
  FETCH_EVENTS,
  ADD_EVENT
} from '../constants/actionTypes';
const initialState = [];

function reducer(state=initialState, action={}) {
  switch(action.type) {

  }
  return state;
};

export default reducer;